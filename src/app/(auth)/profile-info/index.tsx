import ProfileInfoScreen from "@/screens/tabs/profile-settings/ProfileInfoScreen";

type Props = {};

export default function profileinfoname({}: Props) {
  return <ProfileInfoScreen isOnBoarding />;
}
